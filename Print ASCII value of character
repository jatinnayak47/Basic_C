#include<stdio.h>

void checkcode(char c)
{
 printf("ASCII code of %c is %d",c,c);
}

int main()
{
    char ch;
    printf("Enter a character to check :-");
    scanf("%c",&ch);
    checkcode(ch);

    return 0;
}
